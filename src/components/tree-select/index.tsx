/**
 * Tree Select Component
 * 
 * A hierarchical selection component that allows users to navigate through 
 * nested categories with breadcrumb navigation and search functionality.
 */

import TreeSelect from './tree-select';
export { sampleTreeData } from './data';
export type { TreeNode, TreeNodeWithParent, PathItem, TreeSelectProps } from './types';

export default TreeSelect;
